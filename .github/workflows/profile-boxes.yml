name: profile boxes
on:
  schedule:
    - cron: "20 * * * *"
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: refresh gists
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: python3 update_boxes.py
